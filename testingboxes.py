from tkinter import Tk, ttk, filedialog

#def select_file():
    #file_path = filedialog.askopenfilename(title="Select a file")
    #if file_path:
        #file_var.set(file_path)

# Create the main window
#root = Tk()
#root.title("File Selection Window")

# Create a StringVar to store the selected file path
#file_var = ""

# Create an Entry widget to display the selected file path
#file_entry = ttk.Entry(root, textvariable=file_var, state="readonly")
#file_entry.pack(padx=10, pady=10, fill="x", expand=True)

# Create a button to open the file dialog
#select_button = ttk.Button(root, text="Select File", command=select_file)
#select_button.pack(pady=10)

# Run the Tkinter event loop
#root.mainloop()